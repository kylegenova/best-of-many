#ifndef SWAPROUND_H
#define SWAPROUND_H

#include "definitions.h"

vector<vector<LPEdge>> swapRound(const vector<vector<LPEdge>> &trees, const vector<double> &betas);
bool hasCycle(vector<LPEdge> in);
#endif